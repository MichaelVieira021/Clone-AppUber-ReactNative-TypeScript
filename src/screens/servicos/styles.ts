import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    container:{
        flex:1,
        padding: 16,
    },

    titulo:{
        fontSize: 35,
        fontWeight: "700",
        marginBottom: 28,
    },

    descricao:{
        fontSize: 20,
        fontWeight: "700",
        marginBottom: 28,
    }
    
})